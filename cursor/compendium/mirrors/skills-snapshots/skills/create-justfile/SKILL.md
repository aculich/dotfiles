---
name: create-justfile
description: >-
  Alias of dwim-justfile. Scaffolds DWIM justfiles (help, status, doctor, doit).
  Prefer reading ~/.cursor/skills/dwim-justfile/SKILL.md. Use for /create-justfile.
disable-model-invocation: true
---

# Create Justfile → DWIM Justfile

This skill is an **alias**. Canonical instructions live in:

**[`../dwim-justfile/SKILL.md`](../dwim-justfile/SKILL.md)**

**Preferred slash:** `/dwim-justfile` (also `/create-justfile`).

For tool quickstart metarepos, use template:

`../dwim-justfile/templates/tool-quickstart.justfile`

Legacy templates under `templates/` (minimal / umbrella / operational) remain for non-quickstart shapes. Do **not** ship a `doit` that only prints “fill in happy path.”
