# Create a DWIM justfile

Alias of `/dwim-justfile`. Canonical command: [dwim-justfile.md](dwim-justfile.md).

## Implementation

1. Read skill: `~/.cursor/skills/dwim-justfile/SKILL.md`
2. Follow `/dwim-justfile` implementation steps
