[

    
    // https://vspacecode.github.io/docs/bonus/
    // Quick Window Navigation: To navigate all the windows including slide and bottom pane with Ctrl-h/j/k/l, you can merge the following config system's key bindings to your keybindings.json file.
    {
        "key": "ctrl+h",
        "command": "workbench.action.navigateLeft",
        "when": "!terminalFocus && !inQuickOpen && !suggestWidgetVisible && !parameterHintsVisible && !isInDiffEditor"
    },
    {
        "key": "ctrl+j",
        "command": "workbench.action.navigateDown",
        "when": "!terminalFocus && !codeActionMenuVisible && !inQuickOpen && !suggestWidgetVisible && !parameterHintsVisible"
    },
    {
        "key": "ctrl+k",
        "command": "workbench.action.navigateUp",
        "when": "!terminalFocus && !codeActionMenuVisible && !inQuickOpen && !suggestWidgetVisible && !parameterHintsVisible"
    },
    {
        "key": "ctrl+l",
        "command": "workbench.action.navigateRight",
        "when": "!terminalFocus && !codeActionMenuVisible && !inQuickOpen && !suggestWidgetVisible && !parameterHintsVisible && !isInDiffEditor"
    },

    // Quick Navigation for diff view
    {
        "key": "ctrl+h",
        "command": "workbench.action.compareEditor.focusSecondarySide",
        "when": "isInDiffEditor && !isInDiffLeftEditor"
    },
    {
        "key": "ctrl+h",
        "command": "workbench.action.navigateLeft",
        "when": "isInDiffEditor && isInDiffLeftEditor"
    },
    {
        "key": "ctrl+l",
        "command": "workbench.action.compareEditor.focusPrimarySide",
        "when": "isInDiffEditor && isInDiffLeftEditor"
    },
    {
        "key": "ctrl+l",
        "command": "workbench.action.navigateRight",
        "when": "isInDiffEditor && !isInDiffLeftEditor"
    },

    // Trigger vspacecode in empty editor group
    {
        "key": "space",
        "command": "vspacecode.space",
        "when": "activeEditorGroupEmpty && focusedView == '' && !whichkeyActive && !inputFocus"
    },
    // Trigger vspacecode when sidebar is in focus
    {
        "key": "space",
        "command": "vspacecode.space",
        "when": "sideBarFocus && !inputFocus && !whichkeyActive"
    },
    // Keybindings required for edamagit
    // https://github.com/kahole/edamagit#vim-support-vscodevim
    // Cannot be added to package.json because keybinding replacements
    {
        "key": "tab",
        "command": "extension.vim_tab",
        "when": "editorTextFocus && vim.active && !inDebugRepl && vim.mode != 'Insert' && editorLangId != 'magit'"
    },
    {
        "key": "tab",
        "command": "-extension.vim_tab",
        "when": "editorTextFocus && vim.active && !inDebugRepl && vim.mode != 'Insert'"
    },
    {
        "key": "x",
        "command": "magit.discard-at-point",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/"
    },
    {
        "key": "k",
        "command": "-magit.discard-at-point"
    },
    {
        "key": "-",
        "command": "magit.reverse-at-point",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/"
    },
    {
        "key": "v",
        "command": "-magit.reverse-at-point"
    },
    {
        "key": "shift+-",
        "command": "magit.reverting",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/"
    },
    {
        "key": "shift+v",
        "command": "-magit.reverting"
    },
    {
        "key": "shift+o",
        "command": "magit.resetting",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/"
    },
    {
        "key": "shift+x",
        "command": "-magit.resetting"
    },
    {
        "key": "x",
        "command": "-magit.reset-mixed"
    },
    {
        "key": "ctrl+u x",
        "command": "-magit.reset-hard"
    },
    // Extra ref menu support for edamagit with the key "y"
    // Cannot be added to package.json because keybinding replacements
    {
        "key": "y",
        "command": "-magit.show-refs"
    },
    {
        "key": "y",
        "command": "vspacecode.showMagitRefMenu",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode == 'Normal'"
    },
    // Extra refresh menu support for edamagit with the key "g"
    // Cannot be added to package.json because keybinding replacements
    {
        "key": "g",
        "command": "-magit.refresh",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/"
    },
    {
        "key": "g",
        "command": "vspacecode.showMagitRefreshMenu",
        "when": "editorTextFocus && editorLangId == 'magit' && vim.mode =~ /^(?!SearchInProgressMode|CommandlineInProgress).*$/"
    },
    // Easy navigation in quick open/QuickPick
    {
        "key": "ctrl+j",
        "command": "workbench.action.quickOpenSelectNext",
        "when": "inQuickOpen"
    },
    {
        "key": "ctrl+k",
        "command": "workbench.action.quickOpenSelectPrevious",
        "when": "inQuickOpen"
    },
    // Easy navigation in suggestion/intellisense
    // Cannot be added to package.json because of conflict with vim's default bindings
    {
        "key": "ctrl+j",
        "command": "selectNextSuggestion",
        "when": "suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus"
    },
    {
        "key": "ctrl+k",
        "command": "selectPrevSuggestion",
        "when": "suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus"
    },
    {
        "key": "ctrl+l",
        "command": "acceptSelectedSuggestion",
        "when": "suggestWidgetMultipleSuggestions && suggestWidgetVisible && textInputFocus"
    },
    // Easy navigation in parameter hint (i.e. traverse the hints when there's multiple overload for one method)
    // Cannot be added to package.json because of conflict with vim's default bindings
    {
        "key": "ctrl+j",
        "command": "showNextParameterHint",
        "when": "editorFocus && parameterHintsMultipleSignatures && parameterHintsVisible"
    },
    {
        "key": "ctrl+k",
        "command": "showPrevParameterHint",
        "when": "editorFocus && parameterHintsMultipleSignatures && parameterHintsVisible"
    },
    // Easy navigation in code action
    {
        "key": "ctrl+j",
        "command": "selectNextCodeAction",
        "when": "codeActionMenuVisible"
    },
    {
        "key": "ctrl+k",
        "command": "selectPrevCodeAction",
        "when": "codeActionMenuVisible"
    },
    {
        "key": "ctrl+l",
        "command": "acceptSelectedCodeAction",
        "when": "codeActionMenuVisible"
    },
    // Add ctrl+h/l to navigate in file browser
    {
        "key": "ctrl+h",
        "command": "file-browser.stepOut",
        "when": "inFileBrowser"
    },
    // {
    //     "key": "ctrl+l",
    //     "command": "file-browser.stepIn",
    //     "when": "inFileBrowser"
    // },
    {
        "key": "ctrl+l",
        "when": "terminalFocus",
        "command": "-workbench.action.showActivityBar"
    },

    // https://docs.cursor.com/features/generate-commit-message
    {
        "key": "cmd+m",
        "command": "cursor.generateGitCommitMessage"
        // "when": "scm.inputBoxFocus"
    },
    // https://docs.cursor.com/features/generate-commit-message
    {
        "key": "ctrl+shift+m",
        "command": "cursor.generateGitCommitMessage",
        "when": "workbench.scm.active"
    },
    {
        "key": "ctrl+x d",
        "command": "extension.dired.open",
        "when": "editorTextFocus && !inDebugRepl"
    },
    {
        "key": "ctrl+x d",
        "command": "extension.dired.open",
        "when": "inputFocus && editorLangId == 'chat'"
    },
    // Enable Alt+. to jump to definition in terminal
    // This sends the escape sequence \u001b (ESC) followed by . 
    // which is the standard key sequence for "go to definition" in many shells
    {
        "key": "alt+.",
        "command": "workbench.action.terminal.sendSequence", 
        "args": {
            "text": "\u001b."  // ESC + . sequence
        },
        "when": "terminalFocus"
    },
    {
        "key": "alt+b",
        "command": "workbench.action.terminal.sendSequence", 
        "args": {
            "text": "\u001bb"  // ESC + . sequence
        },
        "when": "terminalFocus"
    },
    {
        "key": "alt+f",
        "command": "workbench.action.terminal.sendSequence", 
        "args": {
            "text": "\u001bf"  // ESC + . sequence
        },
        "when": "terminalFocus"
    },
    {
        "key": "ctrl+x ctrl+f",
        "command": "workbench.action.quickOpen"
    },
    {
        "key": "ctrl+x ctrl+f",
        "command": "workbench.action.quickOpen",
        "when": "focusedView == 'workbench.panel.composerViewPane2.view'"
    },
    {
        "key": "ctrl+x ctrl+f",
        "command": "workbench.action.quickOpen",
        "when": "inputFocus && editorLangId == 'plaintext'"
    },
    {
        "key": "ctrl+x ctrl+f",
        "command": "workbench.action.quickOpen",
        "when": "focusedView == 'workbench.panel.aichat.view'"
    },
    // https://marketplace.visualstudio.com/items?itemName=YuTengjing.vscode-classic-experience
    // https://github.com/tjx666/vscode-classic-experience
    { // FIXME: this is not working no matter what I try
        "key": "ctrl+x ctrl+f",
        "command": "workbench.action.quickOpen",
        "when": "!editorFocus && !terminalFocus"
        // "when": "composerBarIsVisible"
        // "when": "aichat.view.focused"
        // "when": "view.workbench.panel.aichat.view.visible && focusedView == 'workbench.panel.aichat.view'"
        // "when": "view.workbench.panel.aichat.view.visible"
    },
    {
        "key": "ctrl+x ctrl+f",
        "command": "workbench.action.quickOpen",
        "when": "inputFocus && editorLangId == 'chat'"
    },
    {
        "key": "ctrl+x ctrl+f",
        "command": "-workbench.action.quickOpen",
        "when": "!terminalFocus"
    },
    {
        "key": "ctrl+shift+m",
        "command": "-editor.action.toggleTabFocusMode"
    },
    {
        "key": "shift+cmd+c",
        "command": "workbench.action.terminal.copyLastCommandAndLastCommandOutput",
        "when": "terminalFocus"
    },

    // https://marketplace.visualstudio.com/items?itemName=ms-vscode.vscode-speech
    {
        "key": "alt+shift+u",
        "command": "workbench.action.chat.startVoiceChat",
        "when": "!voiceChatInProgress"
    },
    {
        "key": "alt+shift+u",
        "command": "workbench.action.chat.stopListeningAndSubmit",
        "when": "voiceChatInProgress"
    },
    {
        "key": "alt+shift+d",
        "command": "workbench.action.editorDictation.start",
        "when": "!editorDictation.inProgress"
    },
    {
        "key": "alt+shift+d",
        "command": "workbench.action.editorDictation.stop",
        "when": "editorDictation.inProgress"
    },
    {
        "key": "shift+alt+c",
        "command": "workbench.action.terminal.copyLastCommand"
    },
    {
        "key": "cmd+r cmd+p",
        "command": "workbench.action.toggleMaximizedPanel"
    },
    {
        "key": "cmd+r cmd+h",
        "command": "workbench.action.maximizeEditorHideSidebar"
    },
    {
        "key": "cmd+;",
        "command": "editor.action.insertSnippet",
        "args": {
            "snippet": "${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}"
        },
        "when": "editorTextFocus"
    },
    {
        "key": "cmd+i",
        "command": "composerMode.agent"
    },
    {
        "key": "cmd+i",
        "command": "composerMode.agent"
    },
    {
        "key": "shift+cmd+space",
        "command": "vspacecode-expanded.space",
        "when": "!editorTextFocus"
    },
    { // pass through Ctrl+X to terminal session
        "key": "ctrl+x ctrl+x",
        "command": "workbench.action.terminal.sendSequence",
        "args": {
            "text": "\u0018\u0018" // send ctrl+x passthru
        },
        "when": "terminalFocus"
    }
]

// NOTES:
// open "x-apple.systempreferences:com.apple.Keyboard-Settings"                  // open Keyboard settings panel
// defaults write -g ApplePressAndHoldEnabled -bool false                         // disable accept popup on press and hold
// defaults write NSGlobalDomain NSAutomaticPeriodSubstitutionEnabled -bool false // disable insert period on double space
// defaults write -g KeyRepeat -int 1                                            // set keyboard repeat rate to fastest
// defaults write -g InitialKeyRepeat -int 10                                    // set key repeat delay to shortest

// Input Menu and Text Settings:
// open "x-apple.systempreferences:com.apple.Keyboard-Settings.textinput"        // open Text Input settings panel
// open "x-apple.systempreferences:com.apple.Language-Region-Settings"           // open Language & Region settings

// Option Key Settings:
// open "x-apple.systempreferences:com.apple.Keyboard-Settings.modifier"         // open Modifier Keys settings
// defaults write -g com.apple.keyboard.modifiermapping.1452-566-0 -array-add '<dict><key>HIDKeyboardModifierMappingSrc</key><integer>30064771129</integer><key>HIDKeyboardModifierMappingDst</key><integer>30064771113</integer></dict>'  // Set Left Option to Esc+

// Check current modifier mappings:
// hidutil property --get "UserKeyMapping"                                      // show current key mappings
// defaults -currentHost read -g com.apple.keyboard.modifiermapping            // read modifier key mappings

// Reset modifier mappings:
// defaults -currentHost delete -g com.apple.keyboard.modifiermapping          // reset all modifier mappings

// defaults write com.apple.TextInputMenu visible -bool false                    // hide input menu from menu bar
// defaults write -g NSAutomaticSpellingCorrectionEnabled -bool false           // disable automatic spelling correction
// defaults write -g NSAutomaticCapitalizationEnabled -bool false               // disable automatic capitalization
// defaults write -g NSAutomaticTextCompletionEnabled -bool false               // disable inline predictive text
// defaults write NSGlobalDomain NSAutomaticPeriodSubstitutionEnabled -bool false // disable period on double-space
// defaults write -g NSAutomaticQuoteSubstitutionEnabled -bool false            // disable smart quotes
// defaults write -g NSAutomaticDashSubstitutionEnabled -bool false             // disable smart dashes

// After changing settings:
// killall SystemUIServer                                                        // restart UI to apply changes

// Open Keyboard Shortcuts Settings:
// open "x-apple.systempreferences:com.apple.Keyboard-Settings.shortcuts"        // open Keyboard Shortcuts panel
// open "x-apple.systempreferences:com.apple.Keyboard-Settings.modifier"         // open Modifier Keys panel

// Read current settings:
// defaults read -g ApplePressAndHoldEnabled                                     // check press-and-hold setting
// defaults read -g KeyRepeat                                                    // check key repeat rate
// defaults read -g InitialKeyRepeat                                            // check key repeat delay
// defaults read -g NSAutomaticSpellingCorrectionEnabled                        // check spelling correction
// defaults read -g NSAutomaticCapitalizationEnabled                            // check auto capitalization
// defaults read -g NSAutomaticTextCompletionEnabled                            // check predictive text
// defaults read -g NSAutomaticQuoteSubstitutionEnabled                         // check smart quotes
// defaults read -g NSAutomaticDashSubstitutionEnabled                          // check smart dashes
// defaults read NSGlobalDomain NSAutomaticPeriodSubstitutionEnabled           // check double-space period

// List all defaults for an app/domain:
// defaults read com.apple.TextInputMenu                                        // read all TextInput settings
// defaults read -g                                                            // read all global settings
// defaults find KeyRepeat                                                     // find all domains containing a key

// Export/Backup current defaults:
// defaults domains | tr ',' '\n' | xargs -I{} sh -c 'echo "Domain: {}"; defaults read {}'  // list ALL defaults
// defaults export com.apple.TextInputMenu /tmp/text_input_defaults.plist      // export specific domain
// defaults export -g /tmp/global_defaults.plist                               // export global defaults

// Note: There's no direct way to see original default values if they've been changed.
// You can either:
// 1. Check on a fresh macOS installation
// 2. Create a new user account to see defaults
// 3. Reset all defaults: defaults delete domain-name


