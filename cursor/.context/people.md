# People

| Name | Role | Notes |
|------|------|-------|
| Aaron Culich | Owner / operator | Dotfiles, Cursor ops, skills backup |
| Cursor Agent | Ops assistant | Read `.context/`, run read-only `just status`; write only when asked |
| launchd `com.user.cursor-skill-backup` | Automation | Daily `just skill-backup` when installed |

## Communication

- Primary: this Cursor workspace chat + SpecStory
- Status surface: `just status` and the ops status canvas
- Meeting tools (Granola/Zoom) are ambient context for other projects — not required for day-to-day Cursor ops here
