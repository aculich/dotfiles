# Attic (archived / low-priority)

Content moved here during dotfiles reorganization; **not** deleted. Includes, for example:

- `codegen-preflight/` — prior codegen preflight snapshot
- `cursor-dotfiles-research.json` — one-off research export
- `preflight-archives/` — version snapshot archive

Add new subfolders as needed when retiring experiments.
