Here's a markdown table that summarizes the provided Git aliases with clear explanations and their corresponding commands:

```markdown
| Alias                  | Explanation                                                                                                       | Command                                                                                   |
|------------------------|-------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------|
| gk                     | Lists all branches and tags, showing detailed information.                                                      | `gitk --all --branches &!`                                                              |
| gke                    | Lists all branches, including commit references from reflogs, with additional details.                          | `gitk --all $(git log --walk-reflogs --pretty=%h) &!`                                   |
| gclones                | Clones repositories from a list of URLs stored in `urls.list`.                                                 | `for url in $(<urls.list); do git clone "$url" "${url:t}__${url:h:t}"; done`          |
| glp                    | Displays the Git log in a more readable format.                                                                 | `_git_log_prettily`                                                                      |
| giglv                  | Shows the `.git/info/exclude` file, which lists ignored files.                                                  | `cat .git/info/exclude`                                                                  |
| clp                    | Clears the terminal, pulls changes using `clasp`, shows uncommitted changes, then displays the status.         | `clear; clasp pull; git diff; gst`                                                       |
| gtl                    | Lists tags sorted by version number in descending order.                                                        | `gtl(){ git tag --sort=-v:refname -n --list "${1}*" }; noglob gtl`                     |
| gro                    | Opens the URL for the remote origin in a web browser.                                                           | `open $(git remote get-url origin)`                                                      |
| grt                    | Changes the working directory to the root of the current Git repository.                                        | `cd "$(git rev-parse --show-toplevel || echo .)`                                        |
| gpullall              | Pulls the latest changes from all subdirectories that are Git repositories.                                       | `for d in */; do (cd "$d" && git pull --all); done`                                     |
| current_branch         | Returns the name of the current branch.                                                                          | `git rev-parse --abbrev-ref HEAD`                                                        |
| delete-local-branches  | Deletes all branches that have been merged, excluding the current branch.                                        | `git branch --merged | grep -v \* | xargs git branch -D`                               |
| g                      | A shorthand for the `git` command.                                                                              | `git`                                                                                     |
| ga                     | Stages changes for the next commit.                                                                              | `git add`                                                                                 |
| gaa                    | Stages all changes, including new files.                                                                        | `git add --all`                                                                           |
| gam                    | Stages a commit's contents from a patch file.                                                                   | `git am`                                                                                  |
| gama                   | Aborts the current `am` operation.                                                                               | `git am --abort`                                                                          |
| gamc                   | Continues the `am` operation after fixing conflicts.                                                            | `git am --continue`                                                                       |
| gams                   | Skips the current patch during an `am` operation.                                                               | `git am --skip`                                                                           |
| gamscp                 | Shows the current patch during an `am` operation.                                                                | `git am --show-current-patch`                                                             |
| gap                    | Applies a patch to the files in the repository.                                                                 | `git apply`                                                                                |
| gapa                   | Stages changes interactively while applying patches.                                                             | `git add --patch`                                                                         |
| gapt                   | Attempts a 3-way merge when applying a patch.                                                                    | `git apply --3way`                                                                        |
| gau                    | Stages modified files for the next commit.                                                                      | `git add --update`                                                                         |
| gav                    | Stages files interactively while displaying information for each file.                                          | `git add --verbose`                                                                        |
| gb                     | Lists all branches in the repository.                                                                            | `git branch`                                                                               |
| gbD                    | Forcefully deletes a specified branch.                                                                           | `git branch --delete --force`                                                              |
| gba                    | Lists all branches including remote branches.                                                                    | `git branch --all`                                                                         |
| gbd                    | Deletes a specified branch after confirmation.                                                                    | `git branch --delete`                                                                      |
| gbg                    | Lists branches that are no longer present in remote repositories.                                               | `LANG=C git branch -vv | grep ": gone]"`                                                 |
| gbgD                   | Forcefully deletes branches that are no longer present in remote.                                                | `LANG=C git branch --no-color -vv | grep ": gone]" | cut -c 3- | awk '{print $1}' | xargs git branch -D`                                                       |
| gbgd                   | Deletes merged branches that are no longer present in remote repositories, keeping unmerged branches.            | `LANG=C git branch --no-color -vv | grep ": gone]" | cut -c 3- | awk '{print $1}' | xargs git branch -d` |
| gbl                    | Blames a specified line in the file.                                                                             | `git blame -w`                                                                            |
| gbm                    | Renames a specified branch.                                                                                     | `git branch --move`                                                                        |
| gbnm                   | Lists branches that have not been merged into the current branch.                                               | `git branch --no-merged`                                                                    |
| gbr                    | Lists remote branches in the repository.                                                                         | `git branch --remote`                                                                       |
| gbs                    | Starts a bisect session.                                                                                       | `git bisect`                                                                               |
| gbsb                   | Marks the current commit as bad during a bisect session.                                                       | `git bisect bad`                                                                           |
| gbsg                   | Marks the current commit as good during a bisect session.                                                      | `git bisect good`                                                                          |
| gbsn                   | Marks the current commit as new during a bisect session.                                                       | `git bisect new`                                                                            |
| gbso                   | Marks the current commit as old during a bisect session.                                                       | `git bisect old`                                                                            |
| gbsr                   | Resets the bisect session to its original state.                                                               | `git bisect reset`                                                                         |
| gbss                   | Starts a bisect session with a specific range.                                                                 | `git bisect start`                                                                         |
| gbsu                   | Sets the upstream tracking branch for the current branch to match the corresponding origin branch.              | `git branch --set-upstream-to=origin/$(current_branch) $(current_branch)`                 |
| gc                     | Commits changes with a verbose message.                                                                         | `git commit --verbose`                                                                      |
| 'gc!'                  | Amends the last commit's message using verbose output.                                                          | `git commit --verbose --amend`                                                             |
| gcB                    | Checks out a branch and creates it, resetting the current branch to a specific state.                                                                              | `git checkout -B`                                                                           |
| gca                    | Commits all changes with a verbose message.                                                                    | `git commit --verbose --all`                                                               |
| 'gca!'                 | Amends the last commit's message after committing all changes with verbose output.                              | `git commit --verbose --all --amend`                                                       |
| gcam                   | Commits with a specific message including all changed and new files.                                            | `git commit --all --message`                                                                 |
| 'gcan!'                | Amends the last commit without editing the message.                                                            | `git commit --verbose --all --no-edit --amend`                                             |
| 'gcann!'               | Amends the last commit marking the date as "now".                                                               | `git commit --verbose --all --date=now --no-edit --amend`                                  |
| 'gcans!'               | Amends the last commit with signoff included.                                                                  | `git commit --verbose --all --signoff --no-edit --amend`                                   |
| gcas                   | Commits all changes with a signoff.                                                                            | `git commit --all --signoff`                                                                 |
| gcasm                  | Commits all changes with a signoff and a message.                                                              | `git commit --all --signoff --message`                                                      |
| gcb                    | Checks out a new branch.                                                                                       | `git checkout -b`                                                                             |
| gcd                    | Checks out a specified branch that pulls from the develop branch.                                             | `git checkout $(git_develop_branch)`                                                         |
| gcf                    | Lists all current configurations in the repository.                                                           | `git config --list`                                                                         |
| gclean                 | Cleans the repository interactively by selecting files to remove.                                               | `git clean --interactive -d`                                                                |
| gclf                   | Clones a Git repository including recursive submodules, while applying shallow filters.                             | `git clone --recursive --shallow-submodules --filter=blob:none --also-filter-submodules`  |
| gcm                    | Switches to the main branch.                                                                                    | `git checkout $(git_main_branch)`                                                             |
| gcmsg                  | Commits changes using a specified message.                                                                     | `git commit --message`                                                                        |
| gcn                    | Commits the changes without editing the message interactively.                                                 | `git commit --verbose --no-edit`                                                            |
| 'gcn!'                 | Amends the last commit's message without editing.                                                              | `git commit --verbose --no-edit --amend`                                                    |
| gco                    | Switches to a specified branch.                                                                                | `git checkout`                                                                              |
| gcom                   | An alias for `gcommit`.                                                                                      | `gcommit`                                                                                    |
| gcoma                  | An alias for `ac`, typically for executing a specific command.                                                | `ac`                                                                                         |
| gcor                   | Checks out files in the working directories and their submodules.                                            | `git checkout --recurse-submodules`                                                         |
| gcount                 | Shows a summary of commits arranged by the number of commits.                                                 | `git shortlog --summary --numbered`                                                         |
| gcp                    | Cherry-picks a commit specified by the user.                                                                   | `git cherry-pick`                                                                           |
| gcpa                   | Aborts the current cherry-pick operation.                                                                     | `git cherry-pick --abort`                                                                    |
| gcpc                   | Continues the cherry-pick operation after resolving conflicts.                                                | `git cherry-pick --continue`                                                                 |
| gcs                    | Signs commits with GPG signature.                                                                             | `git commit --gpg-sign`                                                                      |
| gcsm                   | Commits with a signoff applied along with any specified message.                                              | `git commit --signoff --message`                                                             |
| gcss                   | Commits with both GPG signature and signoff applied.                                                          | `git commit --gpg-sign --signoff`                                                           |
| gcssm                  | Commits with GPG signature, signoff, and a specified message.                                                | `git commit --gpg-sign --signoff --message`                                                 |
| gd                     | Shows changes between the working directory and the index.                                                    | `git diff`                                                                                   |
| gdca                   | Shows differences between the index and the last commit.                                                      | `git diff --cached`                                                                          |
| gdct                   | Shows the latest tag description in the repository.                                                           | `git describe --tags $(git rev-list --tags --max-count=1)`                                  |
| gdcw                   | Shows differences between the working directory and the index, visually displaying changes.                    | `git diff --cached --word-diff`                                                              |
| gds                    | Shows all staged changes.                                                                                     | `git diff --staged`                                                                          |
| gdt                    | Displays a summary of file changes for the last commit.                                                       | `git diff-tree --no-commit-id --name-only -r`                                              |
| gdup                   | Shows differences between the current branch and its upstream branch.                                         | `git diff @{upstream}`                                                                         |
| gdw                    | Displays changes in the working directory against the last commit.                                            | `git diff --word-diff`                                                                        |
| gf                     | Fetches changes from a remote repository.                                                                    | `git fetch`                                                                                   |
| gfa                    | Fetches all branches, tags, and removes deleted branches from remote.                                       | `git fetch --all --tags --prune --jobs=10`                                                  |
| gfg                    | Searches the git files using grep with specific options.                                                    | `git ls-files | grep`                                                                         |
| gfo                    | Fetches changes from the origin remote repository.                                                             | `git fetch origin`                                                                           |
| gg                     | Opens the Git GUI for commit history and management.                                                           | `git gui citool`                                                                             |
| gga                    | Opens the Git GUI to amend the last commit.                                                                   | `git gui citool --amend`                                                                     |
| ggpull                 | Pulls the latest changes from the current branch on the origin remote.                                        | `git pull origin "$(git_current_branch)`                                                   |
| ggpush                 | Pushes changes to the current branch on the origin remote.                                                    | `git push origin "$(git_current_branch)"                                                    |
| ggsup                  | Sets the upstream branch for tracking to origin for the current branch.                                       | `git branch --set-upstream-to=origin/$(git_current_branch)`                                 |
| ghh                    | Displays Git help documentation.                                                                               | `git help`                                                                                    |
| gi                     | Initializes a new Git repository.                                                                             | `git init`                                                                                    |
| gigauv                 | Lists all untracked files or directories in verbose format.                                                 | `git ls-files -v | grep "^[a-z]"`                                                            |
| gignore                | Updates the index to treat specified files as unchanged (not tracked).                                        | `git update-index --assume-unchanged`                                                      |
| gignored               | Lists untracked files and directories.                                                                        | `git ls-files -v | grep "^[[:lower:]]"`                                                     |
| gigwtv                 | Lists all files that are skipped in the working tree.                                                          | `git ls-files -v | grep "^[S]"`                                                              |
| git-svn-dcommit-push   | Performs a dcommit to the SVN and pushes to the main branch in git.                                          | `git svn dcommit && git push github $(git_main_branch):svntrunk`                           |
| gl                     | Pulls updates from the current remote branch without further details.                                          | `git pull`                                                                                    |
| glg                    | Displays the log with stats for each commit.                                                                  | `git log --stat`                                                                             |
| glgg                   | Displays the log in a graphical representation.                                                                | `git log --graph`                                                                            |
| glgga                  | Displays a detailed log with decorations and all branches in a graphical view.                                 | `git log --graph --decorate --all`                                                           |
| glgm                   | Displays the last 10 commits along with their logs.                                                            | `git log --graph --max-count=10`                                                             |
| glgp                   | Displays the log with stats and diffs.                                                                          | `git log --stat --patch`                                                                      |
| glo                    | Shows a one-line summary of the commit history.                                                               | `git log --oneline --decorate`                                                                |
| glod                   | Shows formatted log with commit hashes, tags, and dates.                                                      | `git log --graph --pretty="%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ad) %C(bold blue)<%an>%Creset"` |
| glods                  | Shows formatted logs with dates shortened.                                                                    | `git log --graph --pretty="%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ad) %C(bold blue)<%an>%Creset" --date=short` |
| glog                   | Displays a graph representation of the commit history.                                                        | `git log --oneline --decorate --graph`                                                       |
| gloga                  | Displays a graph with detailed commit information for all branches.                                           | `git log --oneline --decorate --graph --all`                                                |
| glol                   | Displays a formatted log with commit hashes and authors for a clean overview.                                  | `git log --graph --pretty="%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset"` |
| glola                  | Displays a detailed log across all branches with a clean format.                                              | `git log --graph --pretty="%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset" --all` |
| glols                  | Shows a detailed log with stats across all branches.                                                          | `git log --graph --pretty="%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset" --stat` |
| gluc                   | Pulls the latest changes from the upstream for the current branch.                                            | `git pull upstream $(git_current_branch)`                                                  |
| glum                   | Pulls the latest changes from the upstream for the main branch.                                              | `git pull upstream $(git_main_branch)`                                                        |
| gm                     | Merges a specified branch into the current branch.                                                              | `git merge`                                                                                  |
| gma                    | Aborts the current merge operation.                                                                            | `git merge --abort`                                                                           |
| gmc                    | Continues a merge after resolving conflicts.                                                                   | `git merge --continue`                                                                        |
| gmff                   | Ensures that merges happen only when a fast-forward is possible.                                              | `git merge --ff-only`                                                                         |
| gmom                   | Merges the latest changes from the origin's main branch into the current branch.                               | `git merge origin/$(git_main_branch)`                                                       |
| gms                    | Merges changes and squashes commits into a single one.                                                        | `git merge --squash`                                                                        |
| gmtl                   | Launches the merge tool without prompt for resolving conflicts.                                                | `git mergetool --no-prompt`                                                                   |
| gmtlvim                | Launches the merge tool with vimdiff for resolving conflicts.                                                | `git mergetool --no-prompt --tool=vimdiff`                                                  |
| gmum                   | Merges the upstream main branch into the current branch.                                                      | `git merge upstream/$(git_main_branch)`                                                    |
| gp                     | Pushes committed changes to the specified remote.                                                              | `git push`                                                                                   |
| gpd                    | Simulates a push operation to see what would happen.                                                           | `git push --dry-run`                                                                         |
| gpf                    | Pushes with a force that preserves the state of remote branches.                                             | `git push --force-with-lease --force-if-includes`                                         |
| 'gpf!'                 | Forcefully pushes changes to the remote repository.                                                            | `git push --force`                                                                          |
| gpoat                  | Pushes all branches and tags to the origin remote repository.                                                  | `git push origin --all && git push origin --tags`                                          |
| gpod                   | Deletes a specified remote branch from the origin.                                                             | `git push origin --delete`                                                                   |
| gpr                    | Pulls the latest changes with rebasing.                                                                        | `git pull --rebase`                                                                          |
| gpra                   | Pulls with rebasing and automatically stashing changes.                                                       | `git pull --rebase --autostash`                                                             |
| gprav                  | Pulls with rebasing, autostashing, and verbosity for more details.                                            | `git pull --rebase --autostash -v`                                                         |
| gpristine              | Resets the working directory to match the upstream branch, forcing deletion of all untracked changes.         | `git reset --hard && git clean --force -dfx`                                                |
| gprom                  | Pulls the latest changes from the main branch while rebasing.                                                  | `git pull --rebase origin $(git_main_branch)`                                              |
| gpromi                 | Pulls changes interactively from the main branch with rebasing.                                               | `git pull --rebase=interactive origin $(git_main_branch)`                                  |
| gprum                  | Pulls the latest changes from upstream for the main branch.                                                   | `git pull --rebase upstream $(git_main_branch)`                                           |
| gprumi                 | Pulls changes interactively from upstream for the main branch.                                               | `git pull --rebase=interactive upstream $(git_main_branch)`                                 |
| gprv                   | Pulls with verbose output for better logging.                                                                    | `git pull --rebase -v`                                                                     |
| gpsup                  | Pushes to the designated upstream branch for the current local branch.                                         | `git push --set-upstream origin $(git_current_branch)`                                      |
| gpsupf                 | Pushes to the designated upstream branch, forcing changes if necessary.                                         | `git push --set-upstream origin $(git_current_branch) --force-with-lease --force-if-includes` |
| gpu                    | Pushes changes to the upstream remote repository.                                                              | `git push upstream`                                                                          |
| gpv                    | Pushes commits with verbose logging.                                                                           | `git push --verbose`                                                                         |
| gr                     | Manages the remote repositories.                                                                                | `git remote`                                                                                 |
| gra                    | Adds a new remote repository.                                                                                  | `git remote add`                                                                             |
| grb                    | Initiates a rebase operation.                                                                                  | `git rebase`                                                                                |
| grba                   | Aborts the ongoing rebase.                                                                                    | `git rebase --abort`                                                                         |
| grbc                   | Continues a rebase after resolving conflicts.                                                                   | `git rebase --continue`                                                                       |
| grbd                   | Rebases the current branch onto the develop branch.                                                            | `git rebase $(git_develop_branch)`                                                           |
| grbi                   | Starts an interactive rebase session.                                                                          | `git rebase --interactive`                                                                    |
| grbm                   | Rebases the current branch onto the main branch.                                                                | `git rebase $(git_main_branch)`                                                              |
| grbo                   | Rebases using a different base.                                                                                | `git rebase --onto`                                                                          |
| grbom                  | Rebases the current branch onto the origin's main branch.                                                     | `git rebase origin/$(git_main_branch)`                                                      |
| grbs                   | Skips the current patch during a rebase operation.                                                            | `git rebase --skip`                                                                          |
| grbum                  | Rebases the current branch onto upstream's main branch.                                                       | `git rebase upstream/$(git_main_branch)`                                                  |
| grev                   | Reverts changes made in the specified commit.                                                                  | `git revert`                                                                                 |
| greva                  | Aborts the ongoing revert operation.                                                                           | `git revert --abort`                                                                          |
| grevc                  | Continues the revert operation after manual resolution.                                                       | `git revert --continue`                                                                       |
| grf                    | Displays the git reflog for viewing a history of what has happened.                                           | `git reflog`                                                                                 |
| grh                    | Resets the current local branch to the state of the specified commit.                                          | `git reset`                                                                                 |
| grhh                   | Hard resets the local branch, discarding all changes.                                                         | `git reset --hard`                                                                          |
| grhk                   | Resets the local branch while keeping unstaged changes.                                                       | `git reset --keep`                                                                          |
| grhs                   | Soft resets the local branch, keeping changes in the working directory.                                        | `git reset --soft`                                                                          |
| grm                    | Removes specified files from the repository.                                                                  | `git rm`                                                                                    |
| grmc                   | Removes files from the index but keeps them in the working directory.                                          | `git rm --cached`                                                                           |
| grmv                   | Renames a remote repository.                                                                                    | `git remote rename`                                                                          |
| groh                   | Resets the current branch to match the remote repository.                                                     | `git reset origin/$(git_current_branch) --hard`                                             |
| grrm                   | Removes specified remote repositories.                                                                          | `git remote remove`                                                                          |
| grs                    | Restores files to a previous commit state directly.                                                           | `git restore`                                                                               |
| grset                  | Updates the URL of a specified remote repository.                                                             | `git remote set-url`                                                                         |
| grss                   | Restores files from a specific commit or branch.                                                              | `git restore --source`                                                                       |
| grst                   | Restores files to the staged area (index).                                                                     | `git restore --staged`                                                                       |
| gru                    | Resets the current branch to the specified commit, discarding all changes.                                   | `git reset --`                                                                                |
| grup                   | Updates the information from remote repositories.                                                             | `git remote update`                                                                          |
| grv                    | Displays verbose information about remote repositories.                                                       | `git remote --verbose`                                                                       |
| gsb                    | Displays a short status summary along with the current branch.                                                 | `git status --short --branch`                                                                |
| gsd                    | Updates an SVN repository from the current Git branch.                                                        | `git svn dcommit`                                                                            |
| gsh                    | Shows the content of a specific commit.                                                                       | `git show`                                                                                   |
| gsi                    | Initializes git submodules within the repository.                                                              | `git submodule init`                                                                         |
| gsps                   | Shows a short summary of commit changes along with the signature.                                             | `git show --pretty=short --show-signature`                                                  |
| gsr                    | Rebases an SVN repository to match the current state.                                                         | `git svn rebase`                                                                            |
| gss                    | Displays a short status of changes in the repository.                                                           | `git status --short`                                                                         |
| gst                    | Displays the current status of the working directory and index.                                              | `git status`                                                                                 |
| gsta                   | Applies changes currently in the stash.                                                                       | `git stash push`                                                                              |
| gstaa                  | Applies the latest stash and keeps it in the stash list.                                                     | `git stash apply`                                                                            |
| gstall                 | Stashes all changes including ignored files.                                                                     | `git stash --all`                                                                            |
| gstc                   | Clears all stashed changes.                                                                                   | `git stash clear`                                                                            |
| gstd                   | Drops a specific stash item from the stash list.                                                               | `git stash drop`                                                                             |
| gstl                   | Lists out all the current stash items.                                                                          | `git stash list`                                                                             |
| gstp                   | Applies and removes the latest stash.                                                                          | `git stash pop`                                                                              |
| gsts                   | Shows specific details of the stashed changes.                                                                | `git stash show --patch`                                                                    |
| gsu                    | Updates Git submodules to their tip commit.                                                                    | `git submodule update`                                                                        |
| gsw                    | Switches between branches.                                                                                     | `git switch`                                                                                 |
| gswc                   | Creates and switches to a new branch.                                                                         | `git switch --create`                                                                        |
| gswd                   | Switches to the develop branch.                                                                                | `git switch $(git_develop_branch)`                                                            |
| gswm                   | Switches to the main branch.                                                                                  | `git switch $(git_main_branch)`                                                               |
| gta                    | Tags a specific commit with annotations.                                                                       | `git tag --annotate`                                                                         |
| gts                    | Creates a signed tag for commits.                                                                              | `git tag --sign`                                                                             |
| gtv                    | Lists all tags sorted in version order.                                                                        | `git tag | sort -V`                                                                          |
| gunignore              | Updates the index to treat specified files as tracked again.                                                  | `git update-index --no-assume-unchanged`                                                   |
| gunwip                 | Resets the last commit if the commit message contains `--wip--`.                                             | `git rev-list --max-count=1 --format="%s" HEAD | grep -q "\--wip--" && git reset HEAD~1`   |
| gwch                   | Displays a detailed change history based on how commits have evolved.                                          | `git whatchanged -p --abbrev-commit --pretty=medium`                                      |
| gwip                   | Stages all changes and creates a temporary commit indicating work in progress.                                  | `git add -A; git rm $(git ls-files --deleted) 2> /dev/null; git commit --no-verify --no-gpg-sign --message "--wip-- [skip ci]"` |
| gwipe                  | Resets the working tree to a given commit, destroying all changes.                                           | `git reset --hard && git clean --force -df`                                                |
| gwt                    | Manages Git worktree operations.                                                                               | `git worktree`                                                                               |
| gwta                   | Adds a new worktree to the repository.                                                                         | `git worktree add`                                                                           |
| gwtls                  | Lists all worktrees associated with the repository.                                                           | `git worktree list`                                                                          |
| gwtmv                  | Moves a worktree to a new location.                                                                           | `git worktree move`                                                                          |
| gwtrm                  | Removes a specified worktree from the repository.                                                             | `git worktree remove`                                                                        |
| sgrep                  | Searches recursively through files in the repository excluding the git directory and other ignored files.     | `grep -R -n -H -C 5 --exclude-dir={.git,.svn,CVS}`                                        |
| gcl                    | Locally clones a Git repository.                                                                               | `git clone "$url" "${url:t}"; }`                                                             |
| gigau                  | Locally updates the index to treat specified files as unchanged.                                               | `for f in "$@"; do git update-index --assume-unchanged "$f"; done`                       |
| gigl                   | Adds specified files to the Git ignore list.                                                                   | `for f in "$@"; do echo "$f" >> .git/info/exclude; done; giglv`                           |
| gigwt                  | Locally updates the index to skip tracking specified files.                                                     | `for f in "$@"; do git update-index --skip-worktree "$f"; done`                           |

```

This table includes all the aliases provided, accompanied by meaningful explanations that distinguish between similar commands or concepts, ensuring clarity for users who may be new to Git or the shortcuts defined.
