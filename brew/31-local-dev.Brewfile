# Wave 4: local development. OrbStack is the container runtime (Docker CLI and
# compose included). Orgs that need Docker Desktop say so in brew/40-<org>.Brewfile.
# Do NOT add nvm/pyenv/rbenv/asdf: mise is the runtime manager.

cask "orbstack"
brew "direnv"
brew "watchexec"
brew "hyperfine"
